# 图书馆管理系统 - 快速开始指南

## 📋 系统要求

- Python 3.6 或更高版本
- MySQL 5.7 或更高版本
- pip（Python包管理器）

## 🚀 快速启动

### 第一步：安装依赖

**方法1：使用安装脚本（推荐）**
```bash
install.bat
```

**方法2：手动安装**
```bash
pip install mysql-connector-python
```

### 第二步：创建数据库

在MySQL命令行或MySQL Workbench中执行：

```sql
CREATE DATABASE library_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

或者执行提供的SQL脚本：
```bash
mysql -u root -p < init_database.sql
```

### 第三步：配置数据库连接

编辑 `config.py` 文件，修改以下配置：

```python
DB_CONFIG = {
    'host': 'localhost',      # MySQL服务器地址
    'port': 3306,             # MySQL端口
    'user': 'root',           # MySQL用户名
    'password': '你的密码',    # ⚠️ 修改为你的MySQL密码
    'database': 'library_db', # 数据库名称
    'charset': 'utf8mb4',
    'autocommit': True
}
```

### 第四步：启动系统

**方法1：使用启动脚本（推荐）**
```bash
start.bat
```

**方法2：命令行启动**
```bash
python -m library_system.main
```

## 📖 功能说明

### 1️⃣ 图书管理

- **添加图书**：录入新书信息（书名、作者、ISBN、出版社等）
- **删除图书**：根据ID删除图书
- **修改图书**：更新图书信息
- **查询图书**：支持按书名、作者、ISBN搜索
- **查看所有图书**：分页浏览所有图书

### 2️⃣ 读者管理

- **注册读者**：添加新读者信息
- **删除读者**：根据ID删除读者
- **修改读者**：更新读者联系方式等
- **查询读者**：支持按姓名、电话、邮箱搜索
- **查看所有读者**：分页浏览所有读者

### 3️⃣ 借阅管理

- **借书**：创建借阅记录，自动检查库存
- **还书**：归还图书，自动更新库存
- **查询借阅记录**：按读者、图书或记录ID查询
- **超期提醒**：查看所有超期未还的图书

### 4️⃣ 统计分析

- 总借阅次数统计
- 当前借出数量
- 超期图书数量
- 热门图书排行榜（TOP 10）
- 活跃读者排行榜（TOP 10）

## 💡 使用技巧

### 添加示例数据

启动系统后，可以添加一些测试数据：

**添加图书示例：**
```
书名: Python编程从入门到实践
作者: Eric Matthes
ISBN: 978-7-115-42802-8
出版社: 人民邮电出版社
出版日期: 2016-07-01
分类: 编程语言
总册数: 5
```

**注册读者示例：**
```
姓名: 张三
电话: 13800138000
邮箱: zhangsan@example.com
地址: 北京市朝阳区
```

### 常用操作提示

1. **输入验证**：系统会自动验证输入格式，请按照提示输入
2. **返回菜单**：在任何子菜单中输入 `0` 可返回上一级菜单
3. **退出系统**：在主菜单输入 `0` 退出系统
4. **取消操作**：大部分操作可以通过输入空值或直接回车跳过

## 🔧 常见问题

### Q1: 连接数据库失败？
**A:** 检查以下几点：
- MySQL服务是否已启动
- config.py 中的用户名和密码是否正确
- 数据库 library_db 是否已创建
- MySQL端口是否为3306

### Q2: 中文显示乱码？
**A:** 确保：
- 数据库字符集为 utf8mb4
- 终端/命令行编码设置为 UTF-8
- Windows系统可使用 `chcp 65001` 切换编码

### Q3: 无法导入模块？
**A:** 确保：
- 已安装 mysql-connector-python
- 在项目根目录运行程序
- Python版本 >= 3.6

### Q4: 如何重置数据库？
**A:** 
```sql
DROP DATABASE library_db;
CREATE DATABASE library_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```
然后重新启动系统，表会自动创建。

## 📂 项目结构

```
学习/
├── library_system/          # 主程序目录
│   ├── __init__.py         # 包初始化
│   ├── models.py           # 数据模型定义
│   ├── database.py         # 数据库操作类
│   ├── book_manager.py     # 图书管理功能
│   ├── reader_manager.py   # 读者管理功能
│   ├── borrow_manager.py   # 借阅管理功能
│   └── main.py             # 主程序入口
├── config.py               # 数据库配置文件
├── requirements.txt        # Python依赖包
├── install.bat            # 安装脚本
├── start.bat              # 启动脚本
├── init_database.sql      # 数据库初始化SQL
├── README.md              # 项目说明文档
└── 使用说明.md            # 本文件
```

## 🎯 下一步

系统已经准备就绪！现在你可以：

1. 运行 `start.bat` 启动系统
2. 开始添加图书和读者
3. 体验借书和还书功能
4. 查看统计数据

祝你使用愉快！📚✨
